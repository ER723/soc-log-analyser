line-length = 100
target-version = "py310"

[lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # long lines tolerated in places (regex patterns, docstrings)

[lint.per-file-ignores]
"tests/*" = ["F401"]
